# Product Card Generator

<div align="center">

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![React](https://img.shields.io/badge/React-18.3-61DAFB.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

**Автоматизированная система создания карточек товаров для маркетплейсов Wildberries и Ozon**

[Возможности](#-возможности) • [Установка](#-установка) • [API](#-api-документация) • [Архитектура](#-архитектура)

</div>

---

## Возможности

### 🎨 Генератор изображений
- **Перспективная трансформация** — наложение принтов на шаблоны с учётом перспективы
- **Multi-area режим** — вставка нескольких изображений в разные области шаблона
- **PSD поддержка** — работа с многослойными PSD файлами
- **Автодетект углов** — автоматическое определение точек перспективы
- **Пакетная обработка** — генерация сотен карточек за минуты

### 📦 Управление карточками
- **Создание карточек** для Wildberries и Ozon
- **Импорт из Excel/Google Sheets** — массовый импорт товаров
- **Массовое редактирование** — изменение множества карточек одновременно
- **Публикация на маркетплейсы** через официальные API

### 🖼️ Редактор фото
- **AI Inpainting** — удаление объектов с автозаполнением
- **Кадрирование** — обрезка с сохранением пропорций
- **Быстрое создание карточек** из отредактированных фото

---

## Установка

### Требования
- Python 3.10+
- Node.js 18+
- Git

### Быстрый старт

```bash
# Клонирование репозитория
git clone https://github.com/meloch287/Product-Card-Generator.git
cd Product-Card-Generator

# Установка зависимостей бэкенда
pip install -r backend/requirements.txt

# Установка зависимостей фронтенда
cd FRONTEND && npm install && cd ..

# Запуск (Windows)
start.bat

# Или вручную:
# Терминал 1
python backend/run.py

# Терминал 2
cd FRONTEND && npm run dev
```

### Доступ
- **Приложение:** http://localhost:5173
- **API Docs:** http://localhost:8000/docs

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  Generator  │  │   Cards     │  │   Editor    │          │
│  │    Page     │  │    Page     │  │    Page     │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
│         │                │                │                  │
│  ┌──────┴────────────────┴────────────────┴──────┐          │
│  │              React + Zustand + TanStack       │          │
│  └───────────────────────┬───────────────────────┘          │
└──────────────────────────┼──────────────────────────────────┘
                           │ REST API
┌──────────────────────────┼──────────────────────────────────┐
│                        Backend                               │
│  ┌───────────────────────┴───────────────────────┐          │
│  │                  FastAPI                       │          │
│  └───────────────────────┬───────────────────────┘          │
│         ┌────────────────┼────────────────┐                 │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐          │
│  │  Templates  │  │    Cards    │  │  Generate   │          │
│  │   Router    │  │   Router    │  │   Router    │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
│         │                │                │                  │
│  ┌──────┴────────────────┴────────────────┴──────┐          │
│  │              Image Processing                  │          │
│  │  ┌─────────────────┐  ┌─────────────────┐     │          │
│  │  │  Perspective    │  │  PSD Processor  │     │          │
│  │  │  Transformer    │  │                 │     │          │
│  │  └─────────────────┘  └─────────────────┘     │          │
│  └───────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### Структура проекта

```
Product-Card-Generator/
├── backend/                    # FastAPI бэкенд
│   ├── app/
│   │   ├── routers/           # API эндпоинты
│   │   │   ├── templates.py   # Шаблоны
│   │   │   ├── cards.py       # Карточки товаров
│   │   │   ├── folders.py     # Папки с принтами
│   │   │   └── generate.py    # Генерация
│   │   ├── services/          # Бизнес-логика
│   │   │   └── image_service.py
│   │   ├── models.py          # Pydantic модели
│   │   └── storage.py         # Хранилище данных
│   └── run.py                 # Точка входа
│
├── FRONTEND/                   # React приложение
│   └── src/
│       ├── components/        # UI компоненты
│       │   ├── PointEditor/   # Редактор точек
│       │   ├── Preview/       # Превью
│       │   └── Cards/         # Карточки
│       ├── pages/             # Страницы
│       ├── store/             # Zustand store
│       └── api/               # API клиент
│
├── src/                        # Модули обработки изображений
│   ├── perspective_transformer.py  # Перспективная трансформация
│   ├── psd_processor.py           # Обработка PSD
│   ├── color_analyzer.py          # Анализ цветов
│   └── skin_detector.py           # Детекция кожи (для PSD)
│
├── uploads/                    # Загруженные файлы
│   ├── templates/             # Шаблоны
│   ├── prints/                # Папки с принтами
│   └── thumbnails/            # Миниатюры
│
└── output/                     # Результаты генерации
```

---

## API Документация

### Шаблоны

#### Получить все шаблоны
```http
GET /api/templates
```

**Response:**
```json
[
  {
    "id": "template_1",
    "name": "Коврик 50x50",
    "thumbnail_url": "/api/templates/template_1/thumbnail",
    "points": [
      {"x": 100, "y": 100},
      {"x": 400, "y": 100},
      {"x": 400, "y": 400},
      {"x": 100, "y": 400}
    ],
    "is_multi_mode": false,
    "corner_radius": 0,
    "blend_strength": 0.25
  }
]
```

#### Загрузить шаблон
```http
POST /api/templates
Content-Type: multipart/form-data
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| file | File | Изображение (PNG, JPG, PSD) |

#### Получить превью
```http
GET /api/templates/{id}/preview?print_file={path}&corner_radius=10
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| print_file | string | Путь к файлу принта |
| print_files | string | Пути через запятую (multi-mode) |
| corner_radius | int | Радиус скругления |
| blend_strength | float | Сила размытия (0-1) |

#### Автодетект точек
```http
POST /api/templates/{id}/auto-detect
```

---

### Папки с принтами

#### Получить папки
```http
GET /api/folders
```

#### Добавить папку
```http
POST /api/folders
Content-Type: application/json
```

```json
{
  "path": "C:/prints/collection1"
}
```

#### Выбрать через диалог
```http
POST /api/folders/browse
```

---

### Генерация

#### Запустить генерацию
```http
POST /api/generate/start
Content-Type: application/json
```

```json
{
  "template_ids": ["template_1", "template_2"],
  "folder_ids": ["folder_1"]
}
```

#### Статус генерации
```http
GET /api/generate/status
```

**Response:**
```json
{
  "is_running": true,
  "current": 45,
  "total": 100,
  "errors": []
}
```

#### Остановить генерацию
```http
POST /api/generate/stop
```

---

### Карточки товаров

#### Получить карточки
```http
GET /api/cards?marketplace=wildberries&status=draft
```

#### Создать карточку
```http
POST /api/cards
Content-Type: application/json
```

```json
{
  "name": "Коврик для мыши RGB",
  "marketplace": "wildberries",
  "article": "MAT-001",
  "brand": "GameZone",
  "price": 1500,
  "images": ["/uploads/prints/mat1.jpg"]
}
```

#### Обновить карточку
```http
PUT /api/cards/{id}
Content-Type: application/json
```

#### Удалить карточку
```http
DELETE /api/cards/{id}
```

---

### Импорт

#### Предпросмотр Excel
```http
POST /api/import/preview
Content-Type: multipart/form-data
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| file | File | Excel файл (.xlsx) |
| marketplace | string | wildberries / ozon |

#### Выполнить импорт
```http
POST /api/import/execute
Content-Type: application/json
```

```json
{
  "file_id": "import_123",
  "marketplace": "wildberries",
  "selected_rows": [0, 1, 2, 5]
}
```

---

### Маркетплейсы

#### Настройки API
```http
GET /api/marketplace/settings
```

```http
POST /api/marketplace/settings
Content-Type: application/json
```

```json
{
  "wb_api_key": "your_wildberries_token",
  "ozon_client_id": "your_client_id",
  "ozon_api_key": "your_api_key"
}
```

#### Тест соединения
```http
POST /api/marketplace/test-connection
Content-Type: application/json
```

```json
{
  "marketplace": "wildberries"
}
```

---

## Конфигурация

### Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `BACKEND_PORT` | 8000 | Порт бэкенда |
| `FRONTEND_PORT` | 5173 | Порт фронтенда |
| `OUTPUT_DIR` | ./output | Папка результатов |
| `UPLOADS_DIR` | ./uploads | Папка загрузок |

### Настройка API маркетплейсов

<details>
<summary>Wildberries</summary>

1. Войдите в [личный кабинет WB](https://seller.wildberries.ru/)
2. Настройки → Доступ к API → Создать токен
3. Выберите права: "Контент"
4. Скопируйте токен в настройки приложения

</details>

<details>
<summary>Ozon</summary>

1. Войдите в [личный кабинет Ozon](https://seller.ozon.ru/)
2. Настройки → API ключи → Создать ключ
3. Скопируйте Client-Id и Api-Key
4. Вставьте в настройки приложения

</details>

---

## Разработка

### Запуск в режиме разработки

```bash
# Бэкенд с hot-reload
cd backend && python run.py

# Фронтенд с hot-reload
cd FRONTEND && npm run dev
```

### Сборка для продакшена

```bash
# Сборка фронтенда
cd FRONTEND && npm run build

# Результат в FRONTEND/dist/
```

---

## 📝 Лицензия

MIT License - см. [LICENSE](LICENSE)

---

## 🤝 Contributing

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Откройте Pull Request

---

<div align="center">
Made with ❤️ for marketplace sellers
</div>
